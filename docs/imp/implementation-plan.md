# AI Trend Hub 実装待ちタスク

最終更新: 2026-03-05
状態: 未着手（仕様確定後に順次着手）

## 1. P0（MVP成立に必須）

## 1.1 基盤

- [ ] Next.js App Router + TypeScript 初期構成
- [ ] Supabase 接続設定（環境変数と権限分離）
- [ ] DB マイグレーション基盤（SQL 管理）
- [ ] 型生成フロー（DB 型 -> TS）

## 1.2 データモデル

- [ ] `feeds`, `source_items`, `articles`, `topic_groups`, `summaries`, `rank_scores` 作成
- [ ] インデックス最適化（`genre+hash`, `published_at`, `topic_group_id`）
- [ ] RLS ポリシー（公開読取/内部書込）

## 1.3 収集・解析

- [ ] RSS 収集ジョブ (`ingest-feeds`)
- [ ] URL 正規化ユーティリティ
- [ ] 本文抽出器
- [ ] 同ジャンル hash 重複判定
- [ ] 100文字要約 + 批評生成サービス
- [ ] 文字数/禁止語バリデータ

## 1.4 公開機能

- [ ] `GET /api/trends` 実装
- [ ] `GET /api/trends/:id` 実装
- [ ] `GET /api/search` 実装（全文検索）
- [ ] 一覧ページ（スマホ優先）
- [ ] 詳細ページ
- [ ] Topic Group ページ
- [ ] 検索 UI（キーワード入力 + 結果表示）
- [ ] 更新ボタン（手動リロード）
- [ ] 批評折りたたみ UI（既定非表示）
- [ ] 要約モード切替（100/200/300）
- [ ] スマホ軽量表示 + PC分析優先導線

## 1.5 シェア基盤

- [ ] `@vercel/og` 画像 API 実装
- [ ] `metadata` 設定（Open Graph + Twitter Card）
- [ ] OGP キャッシュ戦略
- [ ] 共有文面テンプレート生成
- [ ] 共有プレビュー導線を一覧/詳細に常設
- [ ] 記事サムネイル自動生成（プロバイダ抽象化）
- [ ] 生成失敗時プレースホルダー画像

## 1.6 ランキング

- [ ] `rank_scores` バッチ実装
- [ ] 24h/7d/30d 切替 API
- [ ] スコア内訳表示（管理画面のみ）

## 1.7 通知（初期リリース必須）

- [ ] Web Push 購読導線実装
- [ ] 通知設定テーブル実装
- [ ] 07:00 / 12:00 / 18:00 ダイジェスト配信ジョブ
- [ ] 配信ログ保存と失敗再送

## 1.8 パーソナライズ（初期ハイブリッド）

- [ ] 匿名ユーザー設定のローカル保存（興味分野/要約モード/批評表示）
- [ ] 任意ログイン時のサーバープロファイル同期
- [ ] 匿名設定とログイン設定のマージ処理

## 2. P1（品質と運用）

- [ ] 管理画面（フィード ON/OFF、ジョブ状況）
- [ ] サムネ再生成フラグ（運用向け）
- [ ] 要約監査キュー（日次）
- [ ] 監視・アラート（失敗率/遅延）
- [ ] ユニット/統合/E2E テスト整備
- [ ] 行動ログ日次集計テーブル整備
- [ ] 通知開封率/共有率/要約モード選好のダッシュボード整備
- [ ] PC 専用分析ページ整備

## 3. P2（体験強化）

- [ ] オフライン閲覧（PWA キャッシュ戦略拡張）
- [ ] 検索体験向上（FTS -> vector）
- [ ] 要約生成の A/B テスト基盤（公開後）
- [ ] Google 連携を含む高度パーソナライズ

## 4. 実装順（推奨）

1. DB + RLS + 型生成
2. RSS 収集 + 重複排除
3. 要約/批評 + 品質検証 + 100/200/300
4. 一覧/詳細/TopicGroup UI + 検索 UI + 更新ボタン（SP/TB/PC出し分け）
5. OGP + metadata（共有主役導線）
6. 通知（7/12/18）実装
7. ランキングと運用監視

## 5. 完了条件（DoD）

1. 新着反映が 15 分以内（95%）
2. 100文字要約違反率 1% 未満
3. Topic Group で同ジャンル内グループ遷移可能
4. X/Slack で OGP 意図通り表示
5. 通知（7/12/18）配信成功率 98% 以上
6. 重大障害なしで 7 日連続運用
