# AI Trend Hub 実装判断待ち

最終更新: 2026-03-06（スタック確定）

---

## ✅ 確定済み

### 1. AI プロバイダ戦略
採用:
- Gemini Flash API（通常）→ テンプレ要約（完全障害時）の2段フォールバック
- プロバイダ別プロンプトバリアントを持つ設計にする
- 抽象化レイヤーは #14 サムネイルと共通化する

### 2. Topic Group 判定精度
採用:
- ジャンル横断統合は行わない（動画/ブログ/公式の束ねなし）
- 同ジャンル内のみ、コサイン類似度 0.8 以上で自動統合
- 0.8 未満は別表示（レビューキューなし）
- ベータ版では統合漏れより誤統合を避けることを優先

### 3. 外部統計ソース選定
採用:
- 無料/低コストから開始し、KPI 未達時に有料化

### 4. Realtime 演出実装方式
採用:
- ポーリングなし（自動更新なし）
- ユーザーが更新ボタンを押したときのみ再取得
- Supabase Realtime / SSE は採用しない

### 5. PWA オフライン範囲
採用:
- 一覧 + 直近詳細 30 件

### 6. OGP テンプレート方向性
採用:
- 1 テンプレ + ジャンル色のみ分岐

### 7. 収集元ポリシー
採用:
- Google Alerts 中心 + 重要ソースを限定追加（80/20 スタート）
- 取りこぼし率を見て配分を調整

### 8. 保存ポリシー（法務）
採用:
- 本文キャッシュ: 7 日間
- メタデータ + 要約: 永続保存
- 引用は常にソースリンクを明示

### 9. 管理画面認証方式
採用:
- Firebase Auth（Supabase Auth から変更）
- Internal API: 環境変数シークレットトークン（`x-internal-secret` ヘッダー）

### 10. パーソナライズ方式
採用:
- 匿名開始 + 任意ログイン連携

### 11. 要約モード仕様
採用:
- 100 文字標準 + 200/300 展開

### 12. 通知時刻のカスタマイズ範囲
採用:
- 初期は 07:00 / 12:00 / 18:00 固定
- ユーザー編集は後続フェーズで検討

### 13. モバイル非表示ページ範囲
採用:
- 分析・管理は PC 優先（モバイルは閲覧特化）

### 14. サムネイル画像生成プロバイダ
採用:
- プロバイダ抽象化して切替可能にする（#1 フォールバック層と共通）

### 15. 検索ワード入力の扱い
採用:
- P0 でユーザー検索を実装する
- キーワード検索（記事タイトル・要約に対する全文検索）

### 16. Topic Group 導線優先度
採用:
- カード内補助導線として開始
- 遷移率次第でメイン昇格を検討

---

## 🔲 未確定（なし）

すべての判断項目が確定済み。
