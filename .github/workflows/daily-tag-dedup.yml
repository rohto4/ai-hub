name: daily-tag-dedup

on:
  schedule:
    - cron: '30 2 * * *'  # 毎日 02:30 UTC（日本時間 11:30）
  workflow_dispatch:

jobs:
  daily-tag-dedup:
    runs-on: ubuntu-latest
    steps:
      - name: Run tag dedup job
        env:
          APP_URL: ${{ secrets.APP_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: |
          curl --fail --show-error --silent \
            -X POST \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            "${APP_URL}/api/cron/daily-tag-dedup"
